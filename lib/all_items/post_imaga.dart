import 'package:flutter/material.dart';

class Post_image extends StatelessWidget {
  const Post_image({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/shayx.jpg'),
    );
  }
}
